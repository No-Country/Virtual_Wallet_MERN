export const GenericTitle = ({ title }) => {
  return (
    <section className="genericTitle w-full h-[100px] flex justify-center items-center">
      <h2 className="font-titulo font-bold text-black text-2xl">{title}</h2>
    </section>
  )
}